import { SelectionPanelItem } from "@ui/components/selection-panel/selection-panel-item.slint";

export component SelectionPanel inherits Rectangle {
    in-out property <int> active-selection-index: 0;

    @children
}