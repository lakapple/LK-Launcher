import { LKPalette } from "@ui/style/color-palette.slint";

export component LKText inherits Text {
    font-family: "JetBrains Mono";
    font-size: 12.5px;
    font-weight: 500;
    color: LKPalette.foreground;
}